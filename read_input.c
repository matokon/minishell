#include "mini.h"

int read_input(t_shell *shell)
{
    int exit_status;
    char *input;
    //TODO update path function (mokon)
    while(true)
    {
        input = readline("\033[1;32mminishell$ \033[0m");
        if (!input)
		{

		}
		if (*input)
		{

		}
    }
    return (0);
}